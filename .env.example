# For local development with 'npm run dev', the following container URLs are auto-configured by testcontainers:
# - MONGO_URI (MongoDB connection string)
# - AWS_ENDPOINT_URL (LocalStack endpoint)
# - OIDC_JWKS_URI (Entra OIDC stub JWKS endpoint)
# - OIDC_VERIFY_ISS (OIDC issuer for verification)
# - CW__SQS__CREATE_NEW_CASE_URL (SQS queue URL from LocalStack)
# - CW__SQS__UPDATE_STATUS_URL (SQS queue URL from LocalStack)
#
# You can override these values if needed, but typically they are managed automatically.

NODE_ENV=development
PORT=3001

OIDC_VERIFY_AUD=api://client1

AWS_REGION=eu-west-2
AWS_DEFAULT_REGION=eu-west-2
AWS_ACCESS_KEY_ID=test
AWS_SECRET_ACCESS_KEY=test

CW__SNS__CASE_CREATED_TOPIC_ARN=arn:aws:sns:eu-west-2:000000000000:cw__sns__case_created
CW__SNS__CASE_STATUS_UPDATED_TOPIC_ARN=arn:aws:sns:eu-west-2:000000000000:cw__sns__case_status_updated

OUTBOX_MAX_RETRIES=5
OUTBOX_CLAIM_MAX_RECORDS=2
OUTBOX_EXPIRES_MS=5000
OUTBOX_POLL_MS=250

INBOX_MAX_RETRIES=5
INBOX_CLAIM_MAX_RECORDS=2
INBOX_EXPIRES_MS=5000
INBOX_POLL_MS=250

ENVIRONMENT=local

RULES_ENGINE_URL=https://ephemeral-protected.api.dev.cdp-int.defra.cloud/land-grants-api
RULES_ENGINE_AUTH_TOKEN=replace-with-auth-token
RULES_ENGINE_API_KEY=replace-with-your-developer-api-key
RULES_ENGINE_HEADERS="x-api-key: ${RULES_ENGINE_API_KEY},Authorization: Bearer ${RULES_ENGINE_AUTH_TOKEN}"
